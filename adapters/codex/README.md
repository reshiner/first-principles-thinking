# Codex CLI Adapter / Codex CLI 适配器

Entry point for Codex CLI (`~/.codex/skills/first-principles-thinking/`).
Codex CLI 的入口点。

## Files / 文件

- `fpt.agent.md` — Agent definition in Codex CLI's `.agent.md` format / 代理定义

## Installation / 安装

```bash
bash install.sh --codex
```

This creates a symlink at `~/.codex/skills/first-principles-thinking/` → `adapters/codex/`.
在 `~/.codex/skills/first-principles-thinking/` 创建指向 `adapters/codex/` 的符号链接。